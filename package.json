{
  "name": "varasto-core",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "engines": {
    "node": ">=8.0"
  },
  "scripts": {
    "build": "lerna run build",
    "watch": "lerna run watch",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
